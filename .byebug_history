exit
Student.find(params[:id])
params[:id]
@students.find(params[:id])
students.find(params[:id])
students.find(params[:id]
params :id
params :id.class
:id.class
params:id
params.class
params(:id)
params :id()
params :id
params :id)
Student.find(params :id)
params
Student.find(1)
students.id
Student_id
Student.all.each {|student| student.id } 
Student.all.each {|student| student.id } end
Student.all.each do |student|
Student.id
Student[1]
Student.all
student.id
@students
exit
activate_student_path(1)
activate_student_path
exit
new_student_path
exit
students_new_path
new_student_path
